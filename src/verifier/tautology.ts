// Static tautology detector: constant assertions, x==x, snapshot-only, never references the function. Phase 2.
export function isTautological(_src: string, _fn: string, _lang: string): boolean { throw new Error("Phase 2"); }
